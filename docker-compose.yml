services:
  graphdb:
    image: ontotext/graphdb:10.7.0
    container_name: graphdb_local
    ports:
      - "7200:7200"
    volumes:
      - graphdb-data:/opt/graphdb/home
    restart: unless-stopped

volumes:
  graphdb-data:
    driver: local